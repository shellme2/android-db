<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/panel_bg"
                android:clickable="true"
                android:focusableInTouchMode="true"
                android:padding="20dp"
    >

    <TextView
        android:id="@+id/info_tip_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="输入信息："
        android:textSize="12sp"/>

    <EditText
        android:id="@+id/user_id_etv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/info_tip_tv"
        android:hint="输入ID，此ID唯一"
        android:maxLines="2"/>

    <EditText
        android:id="@+id/user_name_etv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/user_id_etv"
        android:hint="用户姓名"
        android:maxLines="1"/>

    <EditText
        android:id="@+id/user_psw_etv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/user_name_etv"
        android:hint="用户密码"
        android:maxLines="1"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/user_psw_etv"
        android:orientation="horizontal">

        <Button
            android:id="@+id/insert_cancel_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="取消"/>

        <Button
            android:id="@+id/insert_ok_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="确定"/>

    </LinearLayout>
</RelativeLayout>